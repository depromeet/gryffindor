#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔨 빌드 체크 중..."
pnpm build

if [ $? -eq 0 ]; then
    echo "✅ 빌드 성공! push를 진행합니다."
    exit 0
else
    echo "❌ 빌드 실패! push가 중단됩니다."
    echo "빌드 오류를 수정한 후 다시 시도해주세요."
    exit 1
fi 